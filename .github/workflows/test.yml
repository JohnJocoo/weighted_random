name: "Test job"

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: erlef/setup-beam@v1
        id: beam
        with:
          otp-version: '26.2'
          elixir-version: '1.16.3'
      - run: mix deps.get
      - run: mix test
